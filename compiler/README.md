# Fifth Compiler

The Fifth compiler lives in `native/` and generates x86_64 ELF binaries directly.

No Rust, no LLVM, no Cranelift. Just Fifth emitting machine code bytes.

See [native/README.md](native/README.md) for benchmarks and examples.
